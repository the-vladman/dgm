.inner
    a( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="!post.featured" )
        img( ng-src="{{ post.grid_photo.path }}")
    a.hidden-xs.hidden-sm( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="post.featured" )
      img( ng-src="{{ post.cover_photo.path }}")
    a.hidden-md.hidden-lg( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="post.featured" )
      img( ng-src="{{ post.grid_photo.path }}")
    a.tag( ui-sref="front.section({ section : post.section.slug, tag : post.tag.slug })" alt="{{ post.tag.name }}" )
        span( class="tag-icon tag-{{ post.tag.slug }}" )

    .overlay( ng-if="$state.is( 'landing' )" )
        .post-info
            h3
                a( href="{{ post.external_link }}" alt="{{ post.name }}" target="_blank" ng-if="post.external_link" ) {{ post.name }}
                a( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="!post.external_link" ) {{ post.name }}
            p.excerpt.hidden-xs.hidden-sm {{ post.content | excerpt | limitTo:150 }}...
        p.category
            a( ui-sref="front.section({ section : post.section.slug, category : post.category.slug })" alt="{{ post.category.name }}" ) {{ post.category.name }}
        p.author {{ post.author }}
        p.float-right.no-margin
            a.read-more( href="{{ post.external_link }}" alt="{{ post.name }}" target="_blank" ng-if="post.external_link" ) Leer más
            a.read-more( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="!post.external_link" ) Leer más
.post-info( ng-if="!$state.is( 'landing' )" ng-class="{featured:post.featured}" )
    h3
        a( href="{{ post.external_link }}" alt="{{ post.name }}" target="_blank" ng-if="post.external_link" ) {{ post.name }}
        a( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="!post.external_link" ) {{ post.name }}
    p.excerpt.hidden-xs.hidden-sm {{ post.content | excerpt | limitTo:250 }}...
    p.category
        a( ui-sref="front.section({ section : post.section.slug, category : post.category.slug })" alt="{{ post.category.name }}" ) {{ post.category.name }}
    p.author {{ post.author }}
        a.read-more( href="{{ post.external_link }}" alt="{{ post.name }}" target="_blank" ng-if="post.external_link" ) Leer más
        a.read-more( ui-sref="front.post({ section : post.section.slug, category : post.category.slug, tag : post.tag.slug, post : post.slug })" alt="{{ post.name }}" ng-if="!post.external_link" ) Leer más
