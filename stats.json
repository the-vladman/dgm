---
layout: nil
---
{
  "tools": {
    "web": "{{ site.posts | where: 'sub_section', 'herramientas' | where: 'published', true | size }}",
    "apps": "{{ site.posts | where: 'sub_section', 'apps' | where: 'published', true | size }}",
    "other": "{{ site.posts | where: 'sub_section', 'gov_apps' | where: 'published', true | size }}"
  },
  "impact": {
    "cases": "{{ site.posts | where: 'sub_section', 'casos-de-uso' | where: 'published', true | size }}",
    "news": "{{ site.posts | where: 'sub_section', 'noticias' | where: 'published', true | size }}",
    "progress": "{{ site.posts | where: 'sub_section', 'avances' | where: 'published', true | size }}"
  }
}
